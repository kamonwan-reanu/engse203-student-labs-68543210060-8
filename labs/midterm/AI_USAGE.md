# AI Usage Log — สอบกลางภาค

ชื่อ-รหัส: นางสาวกมลวรรณ เรณู  68543210060-8

บันทึกทุกครั้งที่ใช้ AI ระหว่างสอบ

| เวลา | งาน (B1/B2/B3/B4) | ถาม AI ว่าอะไร | ใช้คำตอบส่วนไหน | แก้เอง/ตรวจสอบอย่างไร |
|---|---|---|---|---|
|11:25|B1|ช่วยวิเคราะห์จุดบั๊ก 6 จุดใน DashboardPage.jsx และขอวิธีแก้ไข|ใช้โค้ดแก้ไขบั๊ก 6 จุด เช่น การเปลี่ยนเงื่อนไข pending, !== เป็น === และการใส่ await หน้า resetRequests|ตรวจสอบโค้ดและรันเพื่อดูว่าทำงานถูกต้อง|
|12:30|B2|ขอโค้ดเพิ่มช่องค้นหา Search แยกตาม 4 checkpoint|ใช้โค้ดสร้าง searchQuery state, การกรองข้อมูล และข้อความเตือนกรณีไม่พบข้อมูล| |
|13:20|B3|ขอวิธีเพิ่มปุ่มทำเสร็จใน RequestCard แล้วให้กดแล้วเซฟรอด|ส่วนเงื่อนไขซ่อนปุ่ม completed ใน RequestCard กับฟังก์ชัน handleMarkDone ไปต่อกับ service|ลองกดปุ่ม "ทำเสร็จ" ที่การ์ด แล้วกดรีเฟรชดูว่าสถานะยังเป็น completed อยู่ไหม|
|13:30|B4|ขอโค้ดPriorityBadge.jsx แล้วดักเคสแปลกๆ ด้วย|เอาโค้ด PriorityBadge ทั้งไฟล์ไปวาง แล้วเรียกใช้แทนข้อความดั้งเดิมใน RequestCard|เช็กสถานะว่าขึ้น "เร่งด่วน", "ปกติ" ตรงตามข้อมูลไหมและลองใส่ค่ามั่วดูว่าขึ้น "ไม่ระบุ" ไหม|
| | | | | |
